using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CarapaceBehaviour : MonoBehaviour
{
    /*
    public List<GameObject> Carapace;
    public PygonattPattern pattern; 

    void Start()
    {
        //animatorPygonatt = GetComponent<Animator>();
    }

    private void OnTriggerEnter(Collider other)
    {

        if (other.gameObject.TryGetComponent(out harpoonBrain harpoonBrain))
        {
            bool boolJump = animatorPygonatt.GetBool("Jump");
            Debug.Log("Ça TOUUUUUUUUUUUUUUUUUUUUUUUUUUUUCHEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEE");
            if (!boolJump)
            {
                animatorPygonatt.SetBool("Jump", true);
            }
            else
            {
                Debug.Log("ALLO");
                

                    gameObject.SetActive(false);
                }
            }
        }
    }

    void BoolJump()
    {
        animatorPygonatt.SetBool("Jump", false);
    }
    */
}
